1. On instancie ni les classes abstraites ni les interfaces   
2. Override est utilisée uniquement lorsque la méthode vient d'une interface ou d'une classe abstraite  
3. On implémente une interface et on étend une classe abstraite
4. D'après le diagramme, l'interface VehiculeFactory à 02 méthodes (Le fait qu'il n'y est qu'une seule crée des erreurs dans les autres classes du pattern abstract factory et dans la classe de test PatternsTest.)
5. Le pattern Builder 
     - La classe DirecteurDocument utilise les méthodes dont il ne respecte pas la signature
6. A quoi sert la méthode next qui est dans les classes TroisLignesIterator et UneLigneIterator